# =============================================================================
# Subflow: Tap the Metro dev server URL
# Used when Expo Dev Client launcher appears (after clearState).
# =============================================================================
appId: com.cope.app

---

- tapOn:
    point: "50%,42%"

# Wait for bundle to load
- extendedWaitUntil:
    visible:
      text: "COPE"
    timeout: 30000
